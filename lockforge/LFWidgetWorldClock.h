// LFWidgetWorldClock - "World Clock" widget. Configurable timezone
// (config["timezone"] = "America/Los_Angeles" etc.). Shows the
// current local time in that zone plus the city name. Defaults to
// UTC if no config supplied.

#import "LFLockScreenWidget.h"

NS_ASSUME_NONNULL_BEGIN

@interface LFWidgetWorldClock : LFLockScreenWidget
@end

NS_ASSUME_NONNULL_END

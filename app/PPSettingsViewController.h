// PPSettingsViewController — third tab. Minimal UITableView with a few
// rows: GitHub token (for higher catalog rate limits), about / repo
// link, version, and a "View on GitHub" / report-issue link.
//
// Deliberately tiny so the public release looks finished without
// dragging in a settings framework.

#import <UIKit/UIKit.h>

@interface PPSettingsViewController : UITableViewController
@end

// PPBrowseViewController — second tab. Shows the SerStars/Nugget-Wallpapers
// online catalog as a category-filtered grid. Tapping a tile triggers
// download + import + resize (the same staged pipeline used for local
// imports), then offers an "Apply now" button right in the progress card.

#import <UIKit/UIKit.h>

@interface PPBrowseViewController : UIViewController
@end
